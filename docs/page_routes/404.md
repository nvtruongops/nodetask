<!-- Target FE Component: apps/web/src/features/utility/NotFoundPage.tsx -->
<!-- Target Runtime URL: http://localhost:5173/#/404 -->

# 404 Not Found Page Route Specification (`404.md`)

> **Route ID**: `PAGE_404`  
> **Route Name**: `page.404`  
> **Route Path**: `*` (Catch-all non-existent routes)  
> **Route Type**: `PUBLIC`  
> **Layout Shell**: `PublicLayoutShell`  
> **Specification Version**: `2.0.0`  
> **Status**: `APPROVED`  

---

## 1. Overview & Route ID
- **Route ID**: `PAGE_404` (Dùng cho Analytics, Breadcrumb, Logging, Event Tracking, RBAC)
- **Route Name**: `page.404`
- **Description**: Trang 404 (`404.md`) là màn hình khôi phục lỗi khi người dùng truy cập vào tuyến đường không tồn tại hoặc tài nguyên đã bị xóa. Trang cung cấp thông điệp hướng dẫn khôi phục vị trí và nút dẫn về Trang chủ hoặc Workspace.

---

## 2. Route Config & Navigation Metadata
- **URL Path**: `*` (Match all unmatched URL routes)
- **Access Type**: `PUBLIC`
- **Auth Guard**: `None`
- **Layout Shell**: `PublicLayoutShell`
- **Navigation Metadata**:
  - `sidebar`: `false`
  - `header`: `true`
  - `footer`: `true`
  - `breadcrumb`: `false`
  - `searchable`: `false`
  - `navOrder`: `12`
  - `navGroup`: `"system"`

---

## 3. SEO & Social Share Metadata (SEO Meta Specification)
- **Title Tag**: `<title>404 - Page Not Found</title>`
- **Meta Description**: `Trang bạn yêu cầu không tồn tại hoặc đã bị chuyển dời.`
- **Robots**: `noindex, follow` (Ngăn Google index trang lỗi 404)
- **Canonical URL**: **Omitted** (Quy chuẩn Google SEO: Không gắn thẻ Canonical cho trang 404)
- **OpenGraph Specification**:
  - `og:title`: `404 - Page Not Found`
  - `og:description`: `Không tìm thấy trang yêu cầu.`
- **Twitter Card Specification**:
  - `twitter:card`: `summary`
  - `twitter:title`: `404 - Page Not Found`

---

## 4. Loading Strategy & Code Splitting
- **Lazy Load**: `true` (`React.lazy(() => import('@/features/system/NotFoundPage'))`)
- **Preload Strategy**: `None`
- **Chunk Name**: `chunk-page-404`
- **Priority**: `LOW`

---

## 5. Permission Matrix & Access Control (Access Control & RBAC Permissions)
| System Role | View Access | Primary CTA Link | Notes |
| :--- | :--- | :--- | :--- |
| `GUEST` | **Allowed** | Điều hướng về `/` (Landing Page) | Khách vãng lai |
| `USER` | **Allowed** | Điều hướng về `/workspace` | Người dùng đã đăng nhập |
| `ORG_MEMBER` | **Allowed** | Điều hướng về `/workspace` | Thành viên tổ chức |
| `ORG_ADMIN` | **Allowed** | Điều hướng về `/workspace` | Quản trị viên tổ chức |
| `SYSTEM_ADMIN` | **Allowed** | Điều hướng về `/admin` | Quản trị hệ thống |

---

## 6. API Dependency & Serverpod RPC
- **Linked Backend RPC Endpoints**: None (Pure Client-side Fallback Rendering).
- **Data Caching & Stale Policy**: None.

---

## 7. Page State Machine & UI Transitions
- **State Machine Flow**:
  `IDLE` → `MOUNTED` → `RENDERED`
- **UI State Breakdown**:
  - `IDLE`: Chờ mount component.
  - `RENDERED`: Hiển thị thông báo "404 - Page Not Found" và các nút khôi phục.

---

## 8. Component Inventory & Tree

### Component Inventory List
- `PublicLayoutShell`: Organism bọc Header/Footer.
- `NotFoundCard`: Molecule chứa hình vẽ Zero-Icon ASCII/Text 404 và câu thông báo.
- `ReturnButton`: Atom button dẫn người dùng quay lại vị trí an toàn.

### Component Tree
```text
[NotFoundPageContainer]
├── [SkipToContentLink target="#main-content"]
├── [PublicHeader]
└── [MainContent id="main-content" role="main"]
    └── [NotFoundCard maxWidth="500px"]
        ├── [ErrorCodeDisplay contentKey="system.404.code" text="[ 404 ]"]
        ├── [ErrorTitle contentKey="system.404.title"]
        ├── [ErrorDescription contentKey="system.404.description"]
        └── [ReturnButton target="/" contentKey="system.404.return_home"]
```

---

## 9. Error Mapping & Handling
| Status Code | Trigger Condition | UI Error Content Key | Recovery Action | Logging Tag |
| :--- | :--- | :--- | :--- | :--- |
| `404` | Route không khớp bất kỳ URL nào | `system.404.description` | Hiển thị nút "Return Home" | `SYSTEM_404_NOT_FOUND` |

---

## 10. Acceptance Criteria & QA Scenarios

```gherkin
Scenario: User hits invalid URL
  Given a user navigates to "/non-existent-page-url"
  When the router fails to match any defined route
  Then the 404 Not Found Page is rendered
  And the Canonical Tag is omitted from the page header
  And a "Return Home" button is provided
```

---

## Accessibility (a11y) & Design Tokens
- **a11y Standard**: WAI-ARIA 1.2 (`role="alert"`, focus set to 404 Heading on mount).
- **Design Tokens**: `themeMode: 'dark-only'`, `radius: 0px`, `colorScheme: 'monochrome'`.
