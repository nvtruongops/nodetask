# Serverpod Passwords and Secrets (passwords.yaml)
# Local Working Passwords (Ignored by Git)

development:
  databasePassword: 'postgrespassword'
  redisPassword: ''
  serviceSecret: 'nodetask-dev-service-secret-change-me'
  jwtSecret: 'nodetask-jwt-secret-key-change-me'
  aiApiKey: 'your_gemini_or_openai_api_key'
